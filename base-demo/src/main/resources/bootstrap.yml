spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8080
        file-extension: yaml
        namespace: default  =
        group: DEFAULT_GROUP
        username: nacos
        password: nacos
      discovery:
        server-addr: 127.0.0.1:8080
        namespace: default
        group: DEFAULT_GROUP